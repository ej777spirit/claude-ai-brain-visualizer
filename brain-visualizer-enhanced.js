/**
 * AI Brain Visualizer Pro v2.0
 * Enhanced with: synchronized thought chains, clickable nodes,
 * sequential connections, analytics (weight, vector, derivative), heatmap
 */

const CONFIG = {
  nodeRadius: 0.15,
  maxNodes: 500,
  colors: {
    analysis: { primary: '#00d4ff', glow: '#00d4ff40' },
    synthesis: { primary: '#ff6b35', glow: '#ff6b3540' },
    recall: { primary: '#a78bfa', glow: '#a78bfa40' },
    evaluation: { primary: '#10b981', glow: '#10b98140' },
    inference: { primary: '#f59e0b', glow: '#f59e0b40' }
  }
};

// Full implementation available at: github.com/ej777spirit/claude-ai-brain-visualizer
// See outputs folder for complete codebase